// Copyright The Linux Foundation and each contributor to LFX.
// SPDX-License-Identifier: MIT

import { Router } from 'express';

const router = Router();

// Public project endpoints — placeholder for future public-facing project data
// GET /public/api/projects/:id — get public project details (no auth required)

export default router;
